[package]
name = "blobstore"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["staticlib"]

[profile.release-with-debug]
inherits = "release"
debug = true

[dependencies]
cxx = "1.0"

[build-dependencies]
cxx-build = "1.0"
